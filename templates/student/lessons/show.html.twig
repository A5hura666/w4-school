{# templates/lesson/show.html.twig #}

{% extends 'base.html.twig' %}

{% block title %}{{ lesson.title }}{% endblock %}

{% block content %}
    <div class="container mx-auto px-4 py-6">
        <div class="max-w-6xl mx-auto bg-white dark:bg-gray-800 rounded-lg shadow-lg p-6">
            <h1 class="text-3xl font-bold text-gray-800 dark:text-gray-100">{{ lesson.title }}</h1>
            <p class="text-sm text-gray-500 dark:text-gray-400 mb-4">
                Publié le {{ lesson.createdAt|date('d F Y') }}
                par {{ lesson.getChapterId.getCourseId.getTeacherId.getFirstName }}
                {{ lesson.getChapterId.getCourseId.getTeacherId.getLastName }}
            </p>
            <p class="text-sm text-gray-500 dark:text-gray-400 mb-4">
                <a href="#" class="text-blue-500 dark:text-blue-400 hover:underline">
                    {{ lesson.getChapterId.getCourseId.title }}
                </a>
                >
                <a href="#" class="text-blue-500 dark:text-blue-400 hover:underline">
                    {{ lesson.getChapterId.title }}
                </a>
            </p>

            <div class="text-black dark:text-white max-w-none">
                {{ lesson.content|raw }}
            </div>
        </div>
    </div>
{% endblock %}
